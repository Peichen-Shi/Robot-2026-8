# 实验三 桌面物体自动分类整理（仿真）交付说明（最终版）

文档版本：**最终版** ｜ 生成时间：2026-09-10 22:49

---

## 一、任务与实现概述

RoboMaster EP 在 CoppeliaSim 中完成「视觉识别 → 分类抓取 → 按类投放到左右料盒」的全自动整理：顶部相机识别物体类别与位置，夹爪相机在抓取前二次确认目标，机械臂+夹爪完成抓取与放料，底盘全程沿 X/Y 轴（直角）平移，不与料盒、桌面、目标物或自身发生碰撞。

- **A 类**（蓝色棱柱，位置 1/3/5）→ 左侧料盒（LeftBin，中心 x=0.50, y=-0.90）
- **B 类**（红色圆柱，位置 2/4/6）→ 右侧料盒（RightBin，中心 x=0.50, y=+0.90）
- 位置 4 为空槽位：夹爪相机判定无物体时不抓取，直接驶向下一目标

## 二、一键运行（验收标准 ①）

```bat
:: 方式一：双击
02_代码\start_sim.bat

:: 方式二：命令行
cd 02_代码
python launch_sim.py front out.mp4 en     :: 视角 front|top|side，语言 zh|en
python launch_sim.py top none en          :: none = 只跑不录
python launch_sim.py --no-start           :: 复用已打开的 CoppeliaSim
```
Launch 脚本自动完成：启动 CoppeliaSim 并载入场景 → 等待 ZMQ 端口 23000 → 缺少标定则自动标定 → 运行视觉分类主流程 → 写出日志与汇总。

最近一次冷启动记录（`06_日志与数据/launch_20260910_175817.log`，2026-09-10 18:00）：CoppeliaSim PID=12532，ZMQ 就绪后主流程返回码 0，用时 117.0 s。

## 三、验收对照

完整对照表见 **`00_验收对照表.md`**，结论：

| 验收标准 | 结果 |
|---|---|
| ① 一个 Launch 文件启动整个仿真 | 通过（`02_代码/launch_sim.py`，冷启动日志见 06_日志与数据）|
| ② 连续 5 次抓取成功率 ≥ 4 | 通过（三视角均 5/5 = 100%）|
| ③ 无碰撞、无关节越限 | 通过（4 次运行安全违规 0）|
| ④ 不可达目标停止并报错 | 通过（位置 6 中止并输出英文错误）|
| ⑤ 轨迹/结果/错误日志 | 通过（`06_日志与数据/` 23 个日志文件）|

## 四、目录结构

```
实验三_交付包_视觉分类整理/
├── README_交付说明.md            本文件（总体说明）
├── 00_验收对照表.md              5 项验收标准 × 证据文件 × 实测数据
├── 01_文档/
│   ├── 实验三_开发进度交接_2026-09-10.docx   全过程开发交接（问题定位与结论）
│   ├── 诊断结论_*.md                  5 份专项诊断结论（夹爪/机械臂/底盘/Grid1/全流程）
│   ├── 实验三_布局图.png              场景布局标注图
│   ├── 实验三_视觉识别结果.jpg         俯视相机识别结果
│   ├── 实验三_夹爪视角识别结果.jpg     夹爪相机识别结果
│   └── 图片清单.md                    每张图片的生成时间/视角/用途
├── 02_代码/
│   ├── launch_sim.py / start_sim.bat / launch.yaml   ← Launch 入口
│   ├── vision_pipeline_recorded.py                   ← 主流程（识别+抓取+放料+录制+日志+安全）
│   ├── camera_node.py / detector_node.py / gripper_camera.py / vision_calibrate.py
│   ├── sim_logger.py / safety_monitor.py            ← 日志与安全检查
│   ├── add_grid.py / delete_grid4.py / recolor_classes.py / rebuild_shapes.py / set_bins_x.py / make_layout.py
│   ├── *_min_test.py / grid1_*_test.py              ← 最小验证脚本
│   ├── _历史脚本/                                    ← 早期诊断脚本（保留溯源）
│   └── _运行输出/                                    ← 早期脚本的控制台输出
├── 03_场景/
│   ├── second_imitation.ttt        当前场景（5 物体，位置 4 空，左右料盒 x=0.50，地面网格）
│   └── 场景备份/                   改造前各阶段备份（删 Grid4 前 / 加网格前 / 改形状前 / 改色前 / 原始）
├── 04_消息总线样例/                相机与识别节点的消息样例（图像 + JSON）
├── 05_演示视频/                    14 个视频 + 演示视频清单.md
└── 06_日志与数据/                  轨迹 CSV / 结果 JSON / 错误 LOG / 汇总 JSON / Launch 日志 + 日志说明.md
```

## 五、演示视频

逐个视频的**录制时间 — 完成任务 — 视角 — 结果 — 对应日志**见 `05_演示视频/演示视频清单.md`。推荐观看顺序：

1. `实验三_视觉分类演示_正面_最终版_EN.mp4` —— 最终版全流程（5/5 成功）
2. `实验三_视觉分类演示_俯视_最终版_EN.mp4` —— 俯视看底盘直角走位与料盒安全间距
3. `实验三_视觉分类演示_侧面_最终版_EN.mp4` —— 侧视看举升/前伸/放料姿态
4. `实验三_验收演示_不可达目标中止_EN.mp4` —— 验收标准 ④ 异常中止
5. `实验三_直角走位演示_正面.mp4` / `实验三_旧版_斜线走位演示_侧面.mp4` —— 走位方案前后对比

> 所有视频均保存在 `05_演示视频/`，每个文件独立命名，**未覆盖或修改任何既有视频**；录制脚本每次运行生成的视频文件名由命令行参数指定。

## 六、关键实现要点

1. **夹爪控制**：RoboMaster EP 模型自带夹爪控制器占用 `Prismatic_joint`，直接 `setJointPosition` 会被覆盖；实现改为通过 `gripper_link_respondable` 的整数属性 `signal.target`（1=张开 / 2=闭合 / 0=暂停）驱动，并读取 `signal.state` 确认动作完成（详见 `01_文档/诊断结论_夹爪不动.md`）。
2. **抓取判据**：夹爪闭合至 `checkCollision(左右指节, 目标)` 首次触发即停，既确认接触又不压穿物体。
3. **视觉分类**：HSV 颜色分割 + 轮廓圆度判形（棱柱 ≈0.78，圆柱 ≈0.92）；顶部相机 12 m 高、FOV 12° 近似正交，像素↔世界坐标线性标定残差 ≤1.3 mm。
4. **夹爪相机**：装于夹爪指根前方中心、朝正前方，经视锥检查确认画面内不出现机械臂；仅当目标出现在画面中心附近才判定为可抓取，避免把远处物体误判为当前目标。
5. **走位**：底盘只用沿 X/Y 轴的小步 `setObjectPosition` 平移（严格直角路径），配安全通道 `X_SAFE=0.015`，车头前缘 0.239 m 与箱壁外面 0.275 m 之间保留约 3.6 cm 余量。
6. **放料精度**：按携带位姿与插入位姿之差做在线补偿（`delta_insert` + 实时 `arm_offset()`），落位误差 ≤8 mm（本次实测最大 7.8 mm，多数 <5 mm）。
7. **画面文字**：中文叠加使用 PIL + 微软雅黑（`C:\Windows\Fonts\msyh.ttc`），避免 OpenCV 中文乱码；最终版视频统一使用英文界面（`en` 参数）。

## 七、复现环境

- CoppeliaSim Edu 4.10（`C:\Program Files\CoppeliaRobotics\CoppeliaSimEdu\coppeliaSim.exe`）
- Python 3.13 + `coppeliasim_zmqremoteapi_client 2.0.4` + `opencv-python` + `numpy` + `Pillow`
- ZMQ Remote API 默认端口 23000；若被占用，请先关闭残留的 CoppeliaSim/Python 进程

## 八、已知限制

- 物体分类依赖颜色与形状先验（蓝/红、棱柱/圆柱），未使用学习型检测器；
- 物体位置固定在 6 个标定槽位上，未做任意位姿抓取；
- 地面网格与料盒为非响应（non-respondable）视觉体，底盘间距靠坐标保证而非物理碰撞判定；
- 场景中位置 4 已按要求删除物体，因此最终版流程为 5 次抓取。

---

文件清单：视频 14 个、图片 3 张、日志 23 个、代码与脚本 142 个。